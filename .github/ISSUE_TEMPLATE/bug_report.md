---
name: Bug report
about: Something crashed, or a crawl/filter result is wrong
labels: bug
---

**What happened**

**What you expected**

**Command**
```
unifaculty run ...
```

**Environment** — output of `unifaculty doctor` (it never prints keys):
```
```

**Logs** — `summary.json` and the relevant `events.jsonl` lines (see docs/LOGGING.md for jq recipes). For filter problems also attach the matching `runs/<id>/llm/*.json`.
```
```

- [ ] I removed personal data that isn't needed to show the problem.
